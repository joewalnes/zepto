#!/usr/bin/env perl
# Incomplete/corrupted Perl code - simulates mid-typing scenarios

use strict;
use warnings;

# Unterminated string (user typing)
my $message = "Hello, this string is not closed

# Unterminated regex
if ($name =~ /pattern

# Missing closing brace
sub process_data {
    my ($data) = @_;
    if ($data > 0) {
        print "Processing...

# Incomplete hash
my %config = (
    host => 'localhost',
    port =>

# Incomplete array assignment
my @items = (1, 2, 3,

# Incomplete method chain
my $result = Some::Class->new()->

# Unterminated here-doc
my $text = <<'END';
This here-doc has no terminator
It just keeps going

# Incomplete conditional
if ($count > 0

# Partially typed keyword
fore

# Incomplete use statement
use Data::

# Incomplete sub signature
sub calculate {
    my ($a, $b

# Comment at end without newline
# This is a trailing comment