# Expected syntax highlighting for perl_incomplete.pl
# Generated from Perl grammar
# Format: <TYPE>text</TYPE> where TYPE is the token type

<COMMENT>#!/usr/bin/env perl</COMMENT>
<COMMENT># Incomplete/corrupted Perl code - simulates mid-typing scenarios</COMMENT>

<KEYWORD>use</KEYWORD> <TYPE>strict</TYPE>;
<KEYWORD>use</KEYWORD> <TYPE>warnings</TYPE>;

<COMMENT># Unterminated string (user typing)</COMMENT>
<KEYWORD>my</KEYWORD> <VARIABLE>$message</VARIABLE> <OPERATOR>=</OPERATOR> <STRING>"Hello, this string is not closed</STRING>
<STRING></STRING>
<STRING># Unterminated regex</STRING>
<STRING>if ($name =~ /pattern</STRING>
<STRING></STRING>
<STRING># Missing closing brace</STRING>
<STRING>sub process_data {</STRING>
<STRING>    my ($data) = @_;</STRING>
<STRING>    if ($data > 0) {</STRING>
<STRING>        print "</STRING>Processing<OPERATOR>...</OPERATOR>

<COMMENT># Incomplete hash</COMMENT>
<KEYWORD>my</KEYWORD> <VARIABLE>%config</VARIABLE> <OPERATOR>=</OPERATOR> (
    host <OPERATOR>=></OPERATOR> <STRING>'localhost'</STRING>,
    port <OPERATOR>=></OPERATOR>

<COMMENT># Incomplete array assignment</COMMENT>
<KEYWORD>my</KEYWORD> <VARIABLE>@items</VARIABLE> <OPERATOR>=</OPERATOR> (<NUMBER>1</NUMBER>, <NUMBER>2</NUMBER>, <NUMBER>3</NUMBER>,

<COMMENT># Incomplete method chain</COMMENT>
<KEYWORD>my</KEYWORD> <VARIABLE>$result</VARIABLE> <OPERATOR>=</OPERATOR> Some::Class<OPERATOR>-></OPERATOR><FUNCTION>new</FUNCTION>()<OPERATOR>-></OPERATOR>

<COMMENT># Unterminated here-doc</COMMENT>
<KEYWORD>my</KEYWORD> <VARIABLE>$text</VARIABLE> <OPERATOR>=</OPERATOR> <OPERATOR><</OPERATOR><OPERATOR><</OPERATOR><STRING>'END'</STRING>;
This here<OPERATOR>-</OPERATOR>doc has <KEYWORD>no</KEYWORD> <TYPE>terminator</TYPE>
It just keeps going

<COMMENT># Incomplete conditional</COMMENT>
<KEYWORD>if</KEYWORD> (<VARIABLE>$count</VARIABLE> <OPERATOR>></OPERATOR> <NUMBER>0</NUMBER>

<COMMENT># Partially typed keyword</COMMENT>
fore

<COMMENT># Incomplete use statement</COMMENT>
<KEYWORD>use</KEYWORD> <TYPE>Data::</TYPE>

<COMMENT># Incomplete sub signature</COMMENT>
<KEYWORD>sub</KEYWORD> <FUNCTION>calculate</FUNCTION> {
    <KEYWORD>my</KEYWORD> (<VARIABLE>$a</VARIABLE>, <VARIABLE>$b</VARIABLE>

<COMMENT># Comment at end without newline</COMMENT>
<COMMENT># This is a trailing comment</COMMENT>
