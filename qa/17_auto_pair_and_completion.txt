================================================================================
17 — Auto-Pair & Completion
================================================================================

Covers: auto-pair brackets/quotes, skip-over on matching close, ghost-text
completion (buffer word + snippet + recent + AI), completion menu.

--------------------------------------------------------------------------------

ID:       QA-CPLT-001
NAME:     Typing `(` inserts `()` and places cursor between
PRIORITY: P1
FEATURE:  auto-pair

SETUP
  Preference auto_pairs = ON.

STEPS
  1. Empty buffer.
  2. Type `(`.

VERIFY
  - Buffer shows `()`.
  - Cursor is between `(` and `)`.

--------------------------------------------------------------------------------

ID:       QA-CPLT-002
NAME:     Typing `{` inserts `{}`
PRIORITY: P1
FEATURE:  auto-pair

STEPS
  1. Type `{`.

VERIFY
  - Buffer: `{}`. Cursor between.

--------------------------------------------------------------------------------

ID:       QA-CPLT-003
NAME:     Typing `[` inserts `[]`
PRIORITY: P1
FEATURE:  auto-pair

STEPS
  1. Type `[`.

VERIFY
  - Buffer: `[]`. Cursor between.

--------------------------------------------------------------------------------

ID:       QA-CPLT-004
NAME:     Typing `"` inserts `""`
PRIORITY: P1
FEATURE:  auto-pair quote

STEPS
  1. Type `"`.

VERIFY
  - Buffer: `""`. Cursor between.

--------------------------------------------------------------------------------

ID:       QA-CPLT-005
NAME:     Closing bracket skipped over if already at cursor
PRIORITY: P2
FEATURE:  auto-pair skip

SETUP
  Buffer: `()`. Cursor between.

STEPS
  1. Type `)`.

VERIFY
  - Buffer remains `()`.
  - Cursor moves to after `)`.
  - No duplicate `))`.

--------------------------------------------------------------------------------

ID:       QA-CPLT-006
NAME:     Quote skip-over uses odd-count heuristic (REGRESSION)
PRIORITY: P2
FEATURE:  auto-pair quote
RELATED:  bugs.md P2 "Auto-pair quote skip-over is disabled"

STEPS
  1. Type `"`. Buffer: `""`. Cursor between.
  2. Type `hello` (cursor inside `"hello"`).
  3. Type `"`.

VERIFY
  - Cursor jumps over the closing `"` — result: `"hello"` with cursor
    after `"`.
  - Does NOT produce `"hello""`.

--------------------------------------------------------------------------------

ID:       QA-CPLT-007
NAME:     Auto-pair disabled when toggle off
PRIORITY: P2
FEATURE:  auto-pair pref

STEPS
  1. Palette → "Auto Pairs" toggle OFF.
  2. Type `(`.

VERIFY
  - Only `(` inserted, no `)` added.

--------------------------------------------------------------------------------

ID:       QA-CPLT-008
NAME:     Backspace on auto-pair removes both
PRIORITY: P2
FEATURE:  auto-pair

SETUP
  Buffer: `()`. Cursor between.

STEPS
  1. Press Backspace.

VERIFY
  - Buffer empty.
  - Both characters removed.

--------------------------------------------------------------------------------

ID:       QA-CPLT-009
NAME:     Auto-complete dropdown triggers after pause
PRIORITY: P2
FEATURE:  completion

SETUP
  auto_complete ON. Buffer has `function foo() { const longVar = 1; }`.

STEPS
  1. On a new line, type `long`, pause ~500ms.

VERIFY
  - A dropdown menu appears or ghost text shown.
  - Menu includes "longVar" (from buffer words).

--------------------------------------------------------------------------------

ID:       QA-CPLT-010
NAME:     Tab accepts full completion
PRIORITY: P2
FEATURE:  completion

SETUP
  Ghost text "longVar" after partial "long".

STEPS
  1. Press Tab.

VERIFY
  - "long" expands to "longVar".
  - Cursor positioned after "r".

--------------------------------------------------------------------------------

ID:       QA-CPLT-011
NAME:     Right arrow accepts one char (REGRESSION)
PRIORITY: P3
FEATURE:  completion
RELATED:  bugs.md P2 "Right arrow accepts entire ghost text"

STEPS
  1. Ghost text "longVar" after "long".
  2. Press Right arrow.

VERIFY
  - Partial text extends by one character: "longV" now in buffer.
  - Ghost text shows remaining "ar".
  - Continue pressing Right to accept char-by-char.

--------------------------------------------------------------------------------

ID:       QA-CPLT-012
NAME:     Esc dismisses completion
PRIORITY: P1
FEATURE:  completion

STEPS
  1. Ghost text shown.
  2. Press Esc.

VERIFY
  - Ghost text hidden.
  - Buffer unchanged.

--------------------------------------------------------------------------------

ID:       QA-CPLT-013
NAME:     Up/Down navigates completion menu
PRIORITY: P3
FEATURE:  completion menu

STEPS
  1. Ghost text showing a menu of 3+ options.
  2. Press Down, Up.

VERIFY
  - Selection moves within the menu.
  - Highlighted item updates.

--------------------------------------------------------------------------------

ID:       QA-CPLT-014
NAME:     Enter accepts menu selection
PRIORITY: P3
FEATURE:  completion menu

STEPS
  1. Menu showing. Select a specific item.
  2. Press Enter.

VERIFY
  - Selected completion inserted.
  - Menu closes.

--------------------------------------------------------------------------------

ID:       QA-CPLT-015
NAME:     Completion menu does not break undo/redo (REGRESSION)
PRIORITY: P1
FEATURE:  completion undo
RELATED:  bugs.md P0 "Perl warning on undo/redo near end-of-line"

STEPS
  1. Accept a completion.
  2. Press ⌃Z, then ⌃Y.

VERIFY
  - No stderr warnings.
  - Undo/redo works correctly.

--------------------------------------------------------------------------------

ID:       QA-CPLT-016
NAME:     Snippet completion triggers on known keywords
PRIORITY: P3
FEATURE:  completion snippet

SETUP
  Python file, auto_complete ON.

STEPS
  1. Type `def`.
  2. Pause.

VERIFY
  - Snippet suggestion may appear (e.g. `def name():` template).
  - Accepting fills the template.

--------------------------------------------------------------------------------

ID:       QA-CPLT-017
NAME:     Recent-pick remembered
PRIORITY: P3
FEATURE:  completion recent

STEPS
  1. Accept completion "foo_bar".
  2. Later, type "foo".

VERIFY
  - "foo_bar" is preferred in ghost text (recent provider boost).

--------------------------------------------------------------------------------

ID:       QA-CPLT-018
NAME:     AI completion enabled
PRIORITY: P3
FEATURE:  completion ai
CROSS-REF: see qa/41_ai_completion.txt for the full AI Settings dialog /
  consent / ghost-text test plan (QA-AI-001..010)

SETUP
  AI Completion configured (palette → "AI: Configure" → provider, base
  URL, API key, Test Connection, model, Save) and enabled (palette →
  "AI Completion" → consent prompt → Y).

STEPS
  1. Type in a source file, pause.

VERIFY
  - AI suggestion appears as ghost text (may take longer than buffer
    word completion) — see QA-AI-003 for the full scripted flow.
  - Rate-limited to ~12 requests/minute per docs.

--------------------------------------------------------------------------------

ID:       QA-CPLT-019
NAME:     Completion off by toggle
PRIORITY: P2
FEATURE:  completion pref

STEPS
  1. Palette → "Auto Complete" toggle OFF.
  2. Type a common word prefix.

VERIFY
  - No ghost text, no menu appears.
  - Typing is unaffected otherwise.

--------------------------------------------------------------------------------

ID:       QA-CPLT-020
NAME:     Completion doesn't interfere with paste (REGRESSION)
PRIORITY: P2
FEATURE:  completion paste

STEPS
  1. auto_complete ON.
  2. Paste a long word fragment from clipboard.

VERIFY
  - Paste goes through cleanly.
  - Completion does NOT trigger mid-paste with cascading suggestions.

--------------------------------------------------------------------------------

ID:       QA-CPLT-021
NAME:     Ctrl+Space is context-sensitive (completion vs. palette)
PRIORITY: P2
FEATURE:  completion palette
RELATED:  QA-REG-021

STEPS
  1. On an empty line (cursor at col 0), press Ctrl+Space.
  2. Escape, type a word so the cursor sits right after a word character,
     press Ctrl+Space again.

VERIFY
  - Step 1: the command palette opens (default command list visible).
  - Step 2: the completion menu opens instead — the palette's default
    command list must NOT appear. (Editor.pm ~1292-1312: Ctrl+Space
    triggers completion when the character immediately before the cursor
    is a word character, and opens the palette otherwise.)
  - Editor remains fully responsive to typing afterward either way.
  - Discovered while writing QA-REG-021 (Save As via Ctrl+Space right
    after typing content silently typed the command name into the
    document instead of running it) — that script now uses Ctrl+S
    instead to sidestep the ambiguity.

--------------------------------------------------------------------------------

ID:       QA-CPLT-022
NAME:     Ghost completion renders at the cursor on wrapped lines
PRIORITY: P2
FEATURE:  completion ghost wrap
RELATED:  QA-WRAP-001

STEPS
  1. Open a prose/Markdown file with word wrap on, containing a long line
     that wraps into multiple visual rows.
  2. Position the cursor at the true end of that (wrapped) line and type a
     partial word that word-completes against something already in the
     buffer, so ghost completion triggers.

VERIFY
  - The ghost suggestion renders immediately after the cursor on the
    visual row the cursor is actually on (a continuation/wrapped row) —
    NOT glued to the end of the line's first visual segment.
  - Fixed in Phase 2 (bugs.md P2 "Ghost completion renders offset from
    cursor in wrapped/markdown lines"): `Renderer.pm`'s ghost-render
    branch previously guarded on `!$is_wrap_cont`, so it always rendered
    on visual segment 0 regardless of which segment the cursor was
    actually on. Now it checks whether the cursor's document column falls
    within the CURRENT row's wrap segment range before rendering there.
  - Also fixed: ghost text is truncated by display width (terminal
    columns), not character count, so wide CJK/emoji ghost suggestions no
    longer overflow their available fill area.
  - Unit tests: `tests/renderer.t` ("Ghost text renders on the wrap
    segment containing the cursor", "Ghost text truncates by display
    width, not char count").
